# ~/beautybot/app/handlers/master_panel.py
from aiogram import Router, types, F
from aiogram.filters import Command
from app.utils.db import get_unconfirmed_bookings, get_booking_by_id, update_booking_status, add_loyalty_points, add_blocked_date, remove_blocked_date, set_master_schedule, add_master
import os

router = Router()
MASTER_TG = int(os.getenv("MASTER_ID", "0")) if os.getenv("MASTER_ID") else None

# команда мастера показать pending
@router.message(F.chat.id == MASTER_TG, Command("panel"))
async def master_panel(message: types.Message):
    bookings = await get_unconfirmed_bookings()
    if not bookings:
        await message.answer("🕓 Нет новых заявок.")
        return
    for b in bookings:
        text = (
            f"💅 Заявка #{b['id']}\n"
            f"Клиент: {b['user_id']}\n"
            f"Услуга: {b['service_name']}\n"
            f"Дата/Время: {b['date']} {b['time_start']}-{b['time_end']}\n"
        )
        kb = types.InlineKeyboardMarkup(row_width=2)
        kb.add(
            types.InlineKeyboardButton("✅ Подтвердить", callback_data=f"confirm_{b['id']}"),
            types.InlineKeyboardButton("❌ Отклонить", callback_data=f"reject_{b['id']}")
        )
        await message.answer(text, reply_markup=kb)

# подтверждение
@router.callback_query(F.data.startswith("confirm_"))
async def confirm(callback: types.CallbackQuery):
    booking_id = int(callback.data.split("_",1)[1])
    booking = await get_booking_by_id(booking_id)
    if not booking:
        await callback.answer("Запись не найдена.")
        return
    await update_booking_status(booking_id, "confirmed")
    # начислим бонусы — 10% от цены
    price = booking.get("service_price") or 0
    points = int(price * 0.1) if price else 0
    if points > 0:
        await add_loyalty_points(booking["user_id"], points)
    await callback.message.edit_text(f"✅ Заявка #{booking_id} подтверждена. Клиенту начислено {points} баллов.")
    try:
        await callback.bot.send_message(booking["user_id"], f"🎉 Ваша запись #{booking_id} подтверждена. Начислено {points} баллов.")
    except Exception:
        pass
    await callback.answer()

# отклонение
@router.callback_query(F.data.startswith("reject_"))
async def reject(callback: types.CallbackQuery):
    booking_id = int(callback.data.split("_",1)[1])
    booking = await get_booking_by_id(booking_id)
    if not booking:
        await callback.answer("Запись не найдена.")
        return
    await update_booking_status(booking_id, "rejected")
    await callback.message.edit_text(f"❌ Заявка #{booking_id} отклонена.")
    try:
        await callback.bot.send_message(booking["user_id"], f"😔 Ваша запись #{booking_id} отклонена мастером.")
    except Exception:
        pass
    await callback.answer()

# команда для мастера: заблокировать дату (пример: /block 2025-12-31)
@router.message(F.chat.id == MASTER_TG, Command("block"))
async def block_date_cmd(message: types.Message):
    # ожидаем аргумент: дата в формате YYYY-MM-DD
    parts = message.text.split()
    if len(parts) < 2:
        await message.answer("Использование: /block YYYY-MM-DD (например /block 2025-12-31)")
        return
    date_str = parts[1]
    ok = await add_blocked_date(MASTER_TG, date_str, reason="blocked by master")
    if ok:
        await message.answer(f"✅ Дата {date_str} заблокирована.")
    else:
        await message.answer("Ошибка при блокировке даты.")

# команда /unblock YYYY-MM-DD
@router.message(F.chat.id == MASTER_TG, Command("unblock"))
async def unblock_cmd(message: types.Message):
    parts = message.text.split()
    if len(parts) < 2:
        await message.answer("Использование: /unblock YYYY-MM-DD")
        return
    date_str = parts[1]
    ok = await remove_blocked_date(MASTER_TG, date_str)
    if ok:
        await message.answer(f"✅ Дата {date_str} разблокирована.")
    else:
        await message.answer("Ошибка при разблокировке даты.")
